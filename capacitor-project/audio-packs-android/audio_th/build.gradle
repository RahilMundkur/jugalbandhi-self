plugins {
    id 'com.android.asset-pack'
}

assetPack {
    packName = "audio_th"
    dynamicDelivery {
        // User-driven download — see audio_fr/build.gradle for rationale.
        deliveryType = "on-demand"
        instantDeliveryType = "on-demand"
    }
}
